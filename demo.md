The data ingestion service will generate database. 

For demo purpose I am using the already ingested data. I will show the pipeline in the end of the video.


I given the observation for each data visualization in the markdown below each graph.

The models trained here are not integrated to the pipeline. 

After this, I will demonstrate the model training integrated to pipeline.

I have used the apache airflow to setup the pipeline. The folder structure is this way

Now I will start the pipeline and show the pipeline running.


Thank you.